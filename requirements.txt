# Core runtime dependencies (use conservative version ranges)
fastapi>=0.95,<1.0
uvicorn[standard]>=0.18,<1.0
sqlalchemy>=1.4
pydantic>=1.10,<2.0
jinja2>=3.0
python-multipart>=0.0.6

# Optional / database migrations
alembic>=1.8

# HTTP client / testing
httpx>=0.23

# Development & testing tools
pytest>=7.0
pytest-asyncio>=0.20
coverage>=6.0

# Formatting / linting (optional)
black>=23.1.0
isort>=5.0
flake8>=5.0

# Env vars
python-dotenv>=1.0

# To pin exact, run: `pip freeze > requirements.txt` in your virtualenv
