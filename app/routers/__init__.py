"""Paquete de routers.

Aquí se agrupan los módulos que definen las rutas (endpoints) de la API.
Cada archivo en este paquete expone un `APIRouter` que luego se incluye
en `main.py` mediante `app.include_router(...)`.
"""
